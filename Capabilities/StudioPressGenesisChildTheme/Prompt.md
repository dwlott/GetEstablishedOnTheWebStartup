# StudioPressGenesisChildTheme Prompt

Read [Rules.md](Rules.md) first.

## When to use

- Altitude Pro child-theme CSS overlay (`ges-altitude-custom.css`)
- Enqueue / customization PHP under `geotw-custom/` in WAMP theme
- Front-page widget HTML under `Workspace/LocalWordPressBuild/Widgets/` (when used)

## Checklist

```text
[ ] Theme is Genesis + Altitude Pro child on WAMP
[ ] Repo CSS source: Workspace/LocalWordPressBuild/ges-altitude-custom.css
[ ] ges-theme-css-sync.php run (via WordPressContentUpdate --only)
[ ] Custom PHP saved via WordPress Save when edited in WAMP
[ ] No edits to Genesis parent theme — child overlay only
```

Reference: `Workspace/Reference/WordPressGenesisOperatingGuide/`

Handoff: files synced, `{localUrl}` visual check, next step.
