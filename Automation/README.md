# Automation

Optional starter automation:

- [IndexHealthCheck/](IndexHealthCheck/) — optional index health script

**Mirror refresh:** owned by **MirrorToWindows** (docs-only). Configure paths in
`Workspace/OwnerPreferences.md` before running a mirror pass.

Production WordPress build scripts and host mirror automation are **not** shipped
in this starter.
